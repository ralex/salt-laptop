[Unit]
Description=SSH key agent

[Service]
Type=forking
Environment=SSH_AUTH_SOCK=/run/user/{{ pillar['users'][key]['uid'] }}/ssh-agent.socket
Environment=SSH_AUTH_PID=$MAINPID
ExecStart=/usr/bin/ssh-agent -a $SSH_AUTH_SOCK

[Install]
WantedBy=default.target
